[project]
name = "moj-projekt"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "moj-projekt",
    "requests>=2.32.3",
]

[tool.uv.sources]
moj-projekt = { workspace = true }
